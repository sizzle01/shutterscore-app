import {makeStyles}  from "@material-ui/core/styles";
export const useCategoriesStyles = makeStyles((theme) => ({
    categories:{},
    select:{},
     /**MEDIA QUERIES */

  '@media screen and (max-width: 500px)': {
  }

}))